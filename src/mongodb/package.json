{
  "name": "mongodb",
  "version": "1.0.0",
  "description": "MongoDb demos",
  "main": "index.js",
  "scripts": {
    "docker-up": "docker-compose up",
    "docker-down": "docker-compose down",
    "start": "npm run app && npm run docker-down",
    "app": "ts-node index.ts",
    "watch": "nodemon --watch src/**/*.ts --exec ts-node index.ts",
    "deploy": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "assert": "^1.4.1",
    "mongoose": "^4.11.11",
    "ts-node": "^3.3.0"
  },
  "devDependencies": {
    "@types/assert": "^0.0.31",
    "@types/mongoose": "^4.7.21",
    "@types/node": "^8.0.28",
    "typescript": "^2.5.2"
  }
}
